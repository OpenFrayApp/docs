# yaml-language-server: $schema=../../node_modules/shotlist/dist/recipe.schema.json
#
# Test harness for the played fight-board: the whole console at the Ogre's round-3 turn.
# Not installed anywhere until the board replaces hero-board for the site's shots.
name: fight-test
viewport: { width: 1800, height: 1000 }

setup:
  - use: fight-board

clip: viewport

# The bar counts real seconds, and two runs never take the same time.
check: false
